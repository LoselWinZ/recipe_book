class RecipeIngredient {
   String? amount;
   String? einheit;
   String? id;
   String? ingredientId;
   String? name;

  RecipeIngredient({this.amount, this.einheit, this.id, this.ingredientId, this.name});
}
